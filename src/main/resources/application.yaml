fint:
  application-id: fint-flyt-integration-service
server:
  error:
    include-message: always
spring:
  profiles:
    include:
      - flyt-kafka
      - flyt-logging
      - flyt-postgres
      - flyt-resource-server